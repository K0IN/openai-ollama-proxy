services:
  vllm:
    image: docker.io/vllm/vllm-openai:cu130-nightly
    runtime: nvidia
    ipc: host
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ~/.vllm-cache:/root/.cache/huggingface
    environment:
      - VLLM_API_KEY=change-me
      - HF_TOKEN=
      - HUGGING_FACE_HUB_TOKEN=
      - HUGGINGFACE_HUB_TOKEN=
      - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
    command:
      - cyankiwi/Qwen3.6-35B-A3B-AWQ-4bit
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --api-key
      - change-me
      - --served-model-name
      - Qwen3.6
      - --enable-expert-parallel
      - --language-model-only
      - --max-model-len
      - "65536"
      - --max-num-batched-tokens
      - "8096"
      - --max-num-seqs
      - "1"
      - --reasoning-parser
      - qwen3
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_coder
      - --speculative-config
      - '{"method": "mtp", "num_speculative_tokens": 2}'
      - --moe_backend
      - flashinfer_cutlass
    devices:
      - nvidia.com/gpu=all

  proxy:
    image: ghcr.io/k0in/openai-ollama-proxy:latest
    # build: ..
    restart: unless-stopped
    ports:
      - "11434:11434"
    environment:
      - CONFIG_FILE=/etc/proxy/config.toml
    volumes:
      - ./proxy-vllm-qwen3-35b.toml:/etc/proxy/config.toml:ro
    depends_on:
      - vllm

volumes:
  vllm-cache:
